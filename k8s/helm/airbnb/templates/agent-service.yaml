{{- if .Values.agentService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.agentService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.agentService.name }}
spec:
  replicas: {{ .Values.agentService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.agentService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.agentService.name }}
    spec:
      containers:
      - name: {{ .Values.agentService.name }}
        image: {{ include "airbnb.image" (dict "Values" .Values "image" .Values.agentService.image) }}
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.agentService.service.targetPort }}
        env:
        - name: AGENT_SERVICE_PORT
          value: {{ .Values.agentService.env.AGENT_SERVICE_PORT | quote }}
        envFrom:
        - secretRef:
            name: mongo-session
        - secretRef:
            name: supabase-postgres
        resources:
          {{- toYaml .Values.agentService.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.agentService.service.targetPort }}
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.agentService.service.targetPort }}
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.agentService.name }}
  namespace: {{ .Values.global.namespace }}
spec:
  type: {{ .Values.agentService.service.type }}
  ports:
  - port: {{ .Values.agentService.service.port }}
    targetPort: {{ .Values.agentService.service.targetPort }}
  selector:
    app: {{ .Values.agentService.name }}
{{- end }}
